# Project paths (${code_path:}, ${working_path:} resolvers)
deps: ${code_path:./deps}
data: ${code_path:./data}
minidata: ${code_path:./minidata}
code_dir: ${code_path:}
working_dir: ${working_path:""}
minilog: ${code_path:./miniexperiments}
